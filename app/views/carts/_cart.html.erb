<%= link_to(carts_show_path, class: "btn btn-outline-dark") do %>
  <i class="bi-cart-fill me-1"></i>Cart
  <span id="cart-items-count" class="badge bg-dark text-white ms-1 rounded-pill"><%= total_items_in_cart %></span>
<% end %>
